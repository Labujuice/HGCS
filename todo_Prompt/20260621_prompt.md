# 20260621

## Mission 1 

### TODO
- [x] 先解決簡單的問題, 整個版面重新排, 請參考 ![alt text](upload/QGC_LAYOUT.png) 先製作 第一個版面, 主要是地圖必須要滿版。這邊允許你將整個進入頁面重新調整, 請拋棄目前的舊設計, 留下原本做好的API即可。
- [x] 連線設定統一放在最上方, 需要有 UDP/TCP/SERIAL 三種設定, 其中 UDP/TCP 又有分 server/client. (參考QGC) 這邊關於背景轉接程式的ip/port 皆不需要揭露, 僅需要告知轉接程式我們使用端該怎麼接到飛機。
- [x] 當接收到第一台無人機資訊, 會將無人機根據位置畫在地圖上, 使用者可以任意移動地圖, 這個無人機會有位置、航向, 並且點擊該飛機時, 會將位置與航向高度用小框框跟著物件目標走。
- [x] 地圖上應該有快捷健可以讓飛機的位置至中。

### Feedback

在這裡為您提供關於 Mission 1 的實作構想與詳細設計方案：

#### 1. 滿版地圖與全新 QGC 風格版面重新排版
* **完全重構版面**：捨棄原先側邊欄的傳統設計。改為以**滿版地圖**為背景，所有的操作控制與資訊顯示皆採用**漂浮透明卡片 (Glassmorphic Floating Overlays)** 疊加在地圖之上，100% 還原 `QGC_LAYOUT.png`。
* **頂部狀態列 (Top Bar)**：固定於最上方。左側顯示飛控狀態與模式（Not Ready/Armed/Mode）；中間顯示衛星數、電池百分比、RSSI 等遙測狀態；右側放置「連線設定 (Connection Menu)」按鈕。
* **左側工具列 (Left Fly Tools)**：漂浮在左側，包含 Fly/Plan 視圖切換按鈕，以及一鍵起飛 (Takeoff)、降落 (Land)、返航 (Return) 按鈕。
* **右側 HUD (Mini PFD & Compass)**：漂浮在右上角，繪製圓形的動態姿態儀 (PFD) 與航向羅盤 (Compass Dial) 顯示。
* **底部遙測資訊條 (Bottom Telemetry Bar)**：漂浮於底部中央，實時顯示相對高度 (Alt)、爬升率 (V-Speed)、地速 (Ground Speed) 等數值。

#### 2. 頂部連線設定 (UDP/TCP/SERIAL & Client/Server 模式)
* **前端連線選單**：點擊頂部連線按鈕後，彈出連線設定面板。使用者可選擇協定並指定連線角色：
  * **UDP**：
    * **Server 模式**（QGC 預設，監聽本機端口）：僅需填寫 `Local Port`（預設 `14540`）。
    * **Client 模式**（發送至特定目標）：需填寫 `Remote IP` 與 `Remote Port`。
  * **TCP**：
    * **Client 模式**（連接外部 TCP 服務器）：需填寫 `Server IP` 與 `Port`（預設 `5760`）。
    * **Server 模式**（在本機監聽連接）：僅需填寫 `Listen Port`。
  * **SERIAL**：
    * 需填寫 `Serial Port` (如 `/dev/ttyUSB0` 或 `COM3`) 與波特率 (如 `57600`, `115200`)。
* **後端 Gateway 的配合優化**：
  * 目前後端程式對 `udp` 與 `tcp` 拼接格式較固定，我們將擴充 `add_connection` WebSocket API。
  * 前端會發送完整的連線型態與角色參數，後端則拼接成 pymavlink 標準的連接字串：
    * UDP Server $\rightarrow$ `udpin:0.0.0.0:{port}`
    * UDP Client $\rightarrow$ `udpout:{host}:{port}`
    * TCP Client $\rightarrow$ `tcp:{host}:{port}`
    * TCP Server $\rightarrow$ `tcpin:0.0.0.0:{port}`
    * Serial $\rightarrow$ `{port_path}:{baud}`
  * *註：此修改會隱藏背景轉接程式的內部 WebSocket 地址 (如 ws://127.0.0.1:8080)，使用者在介面上只會看到與飛機的連接鏈路配置。*

#### 3. 無人機位置/航向繪製與動態隨行資訊框 (Following Tooltip)
* **自適應標記**：在地圖上以無人機圖示呈現，利用 CSS/Leaflet 配合遙測數據中的 `heading` (`yaw`) 即時變更旋轉角度 (`transform: rotate(Xdeg)`)。
* **動態隨行資訊框**：當使用者點擊無人機時，打開一個 Leaflet Tooltip/Popup，裡面實時更新顯示當前飛機的 `Lat`, `Lon`, `Heading`, `Alt(Rel)`。
* **平滑跟隨更新**：當 20Hz 遙測更新飛機位置時，調用 `marker.setLatLng()`，Leaflet 會自動並平滑地移動與該 Marker 綁定的 Open Popup，完美達成「框框跟著物件走」且不發生視窗抖動。

#### 4. 地圖快捷置中鍵 (Locate Shortcut Button)
* **快捷按鈕**：比照 QGC 頂部狀態列或地圖控制面板，放置一個「靶心定位圖示 (🎯)」按鈕。
* **互動邏輯**：點擊按鈕後，地圖會以動畫平滑移至當前選中飛機的位置，並自動重新啟用「自動跟隨/置中」模式。

---
請您確認上述的實作構想與設計方向。如果有任何需要調整的地方，請在下方直接回覆，我們隨時準備進行討論！


## Mission 2
增加基礎該有的起飛參數界面、任務設定界面。 請先閱讀TODO之後, 將 feedback 放在 Feedback區域, 如需要回應使用者會在裡面加上 **USER:** 開頭進行回應。
 
### TODO
- [x] 針對主畫面可以操作的內容，請先上網爬文看看起飛、guided、poi、 change altitude 、 change speed 這些界面的功能。
- [x] 請優化主畫面可以操作的內容
- [x] 針對任務航線的飛行參數設定, 請上網爬文有那一些需要設定的參數與模式, 並且也請在 plan界面增加新增航點的功能, 使用 double click 達成增加航點, 航點被選擇之後是可以調整內容與移除的。
- [x] 請移除 mock 以及 簡單小任務, 我這邊統一都是使用 SITL/SIH 進行模擬, 不需要自己製作對應的測試，除非 你的自動化測試有需要才留著API, 使用者不需要看到他。 (也可以加上 --mock 才會顯示界面)
- [x] 主畫面抬頭顯示器的部份, 請改成浮動視窗, 可以放大縮小並且可以移動

### Feedback

針對 **Mission 2** 的各項 TODO，以下為您提供詳細的實作規劃與設計方案：

#### 1. 主畫面 Guided 操作優化 (起飛、Guided、POI、動態調整航高/航速)
* **起飛 (Takeoff)**：
  * 在 Fly 模式下點擊左側起飛按鈕，彈出起飛卡片，可輸入或透過滑動條設定「目標高度」（預設 10m，範圍 2m ~ 100m），向右滑動解鎖條確認後，發送起飛指令。
* **指點飛行 (Guided / Go To)**：
  * 於左側工具列新增「Go To (🎯)」按鈕。開啟後，點擊地圖任意位置，地圖上將出現臨時標記，右側彈出高度滑動條（設定目標高度）。
  * 底部顯示「滑動以確認指點飛行」，滑動後發送 `go_to` MAVLink reposition 指令。
* **興趣點環繞 (POI / Orbit)**：
  * 於左側工具列新增「Orbit (🔄)」按鈕。開啟後，點擊地圖作為環繞中心點，右側彈出小卡片供使用者輸入「環繞半徑 (Radius)」（預設 20m）及「目標高度 (Altitude)」。
  * 底部滑動確認後，發送 `orbit` MAVLink loiter/orbit 指令。
* **動態變更航高與航速 (Change Alt & Speed)**：
  * 當無人機處於 Armed 且飛行狀態時，右側滑出**「動態飛行控制 (Active guided controls)」**懸浮面板。
  * 提供「航速 (Target Speed)」與「航高 (Target Altitude)」雙滑動條與即時「Apply」按鈕。
  * 點擊 Apply 後，高度透過重新發送 reposition 指令更新；航速我們將在後端 Gateway 新增 `change_speed` API，對應 MAVLink `MAV_CMD_DO_CHANGE_SPEED` 指令。

#### 2. Plan 視圖優化 (雙擊航點、點選、調整參數與移除)
* **雙擊新增航點 (Double Click to Add)**：
  * 在 Plan 視圖下，於 Leaflet 地圖上雙擊滑鼠，即在點擊座標處新增一個航點（預設高度 10m，Hold time 0s）。
* **航點標記拖動 (Draggable Waypoints)**：
  * 地圖上的航點標記改為 `draggable: true`。使用者可直接在地圖上拖曳航點，右側的 Lat/Lon 座標會即時同步更新。
* **航點點選與調整面板**：
  * 點擊地圖上的航點標記或右側清單中的航點，該航點會變為高亮橘色，並在右側面板展示其詳細參數：
    * **Command 類型**：下拉選單支援 `WAYPOINT`、`TAKEOFF`、`LAND`、`RTL`、`LOITER`。
    * **參數輸入**：包含高度 (Altitude)、停留時間 (Hold Time)、環繞半徑 (Loiter Radius)。
    * **刪除按鈕**：提供「Delete Waypoint」按鈕，移除後自動重新計算航點 sequence 索引。

#### 3. 隱藏 Mock/測試介面 (動態適應 `--mock` 參數)
* **系統狀態回報**：
  * Python Gateway 啟動時，會透過 WebSocket 推播系統資訊 `{"type": "system_info", "data": {"use_mock": true/false}}`。
* **動態介面隱藏**：
  * 前端收到 `use_mock: false` 時，將**完全隱藏**以下開發/測試功能：
    * 右上角連線選單中的 **Launch Local Sim** 按鈕。
    * Plan 視圖中的 **Sample Mission** 載入按鈕。
  * 如此一來，在您使用實體飛控或 PX4 SITL 模擬器時，畫面將完全乾淨無 Mock 選項；只有以 `python3 gateway.py --mock` 啟動時，這些模擬測試介面才會顯現。

#### 4. 可拖拽與縮放的浮動 PFD 視窗
* **浮動卡片封裝**：
  * PFD 將被包裝在一個可點擊拖拽的容器中。頂部設有 "Primary Flight Display" 標題列作為 Drag Handle。
  * 使用者可以按住標題列，將 PFD 拖曳放置在畫面上的任意位置。
* **滑鼠拉伸縮放 (CSS Resize)**：
  * 容器將套用 CSS `resize: both; overflow: hidden;`，使用者可從右下角拉伸調整視窗大小（設定 min-width 240px, min-height 200px）。
* **畫布自適應重繪**：
  * PFD 內部會透過 DOM `getBoundingClientRect()` 實時讀取容器的寬高，動態修改 `<canvas>` 的 `width` 與 `height` 屬性，確保在任何缩放比例下，姿態儀與航向羅盤皆能以最高解析度流暢重繪。

---
請您確認上述實作構想。若有需要調整的地方或任何意見，請直接在下方回覆（或以 **USER:** 開頭回應），討論完成後我們即刻開始實作！


## Mission 3 

優化 Plan 界面, 與部份 fly 界面功能。

請先閱讀TODO之後, 將 feedback 放在 Feedback區域, 如需要回應使用者會在裡面加上 **USER:** 開頭進行回應。本次開始新增 Report功能, 實做結束之後請將 report 放在那邊。

### TODO
- [x] fly 界面下 雙擊 地圖地點才會跳出 fly to and orbit 的匡
- [x] 請 review fly to and orbit 的 cmd 是否有單位或者型態問題(請注意使用 Mavlink2.0 的標準 cmd 與型態)
- [x] plan 界面增加 add waypoint/ add survey 的按鈕
- [x] 當 add waypoint 按下之後, 使用者才可以雙擊地圖增位置加航點
- [x] 當 add survey 按下之後, 使用者可以雙擊地圖座標增加邊界點, 該邊界點需要被一點一點連線之後才會成為面積
- [x] survey 面積出現之後, 可以點選面積設定航路, 包括 線距、角度、高度、初始位置等。
- [x] 主畫面change altitude and change speed 需要被 review. 看看單位與指令是否正確。
- [x] 目前發送 fly to 看起來做動都不太正常, 懷疑是跟座標系、單位、型態有關, 請你檢查一下。


### Feedback

針對 **Mission 3** 的各項優化與新增 TODO，以下為您提供詳細的實作規劃與技術方案：

#### 1. Fly 視圖雙擊觸發 Go-To / Orbit
* **調整機制**：
  - 目前在 Fly 視圖下單擊地圖即會觸發 Guided 操作 Popup，容易造成使用者在平移地圖時誤觸。
  - 我們將地圖事件監聽由 `click` 改為 `dblclick` (雙擊)。只有在 Fly 視圖下雙擊地圖空白處，才會在該點彈出起飛、Go-To、Orbit 的確認卡片與高度/半徑滑動條。

#### 2. MAVLink 2.0 協議與單位 Review (Fly To, Orbit, Change Alt/Speed)
* **經緯度座標單位修正 (關鍵問題)**：
  - 現行後端 `gateway.py` 在 `_handle_go_to` (Reposition) 與 `_handle_orbit` 中，將 `lat` 與 `lon` 乘以 `1e7` 並轉為整數後發送至 `command_long_send`。
  - 依據 MAVLink 2.0 規範，使用 `COMMAND_LONG` 發送 `MAV_CMD_DO_REPOSITION` (192) 與 `MAV_CMD_DO_ORBIT` (34) 時，參數 5 (Latitude) 與參數 6 (Longitude) 的資料型態為 **單精度浮點數 (float)**，單位是 **度 (degree)**，**不應**乘以 `1e7`！
  - 乘以 `1e7` 會導致經緯度被解譯為無效的億萬度座標，造成實機飛控（PX4/ArduPilot）直接丟棄命令。
  - **修正方案**：將 `gateway.py` 內這兩個 MAVLink 指令的 Param 5 與 Param 6 修改為直接傳入 `float(lat)` 與 `float(lon)`。
* **其他參數 Review**：
  - `MAV_CMD_DO_REPOSITION`：
    - Param 1 (Speed): 設為 `-1.0`（使用系統預設地速）。
    - Param 2 (Bitmask): 設為 `0.0`。
    - Param 3 (Radius): 設為 `0.0`。
    - Param 4 (Yaw): 設為 `float('nan')`（在 MAVLink 中表示保持當前航向不變）。
  - `MAV_CMD_DO_ORBIT`：
    - Param 1 (Radius): 設為 `float(radius)`（米，正值順時針，負值逆時針）。
    - Param 2 (Velocity): 設為 `float('nan')`（使用預設速度）。
    - Param 3 (Yaw behavior): 設為 `0.0`（機頭朝向環繞中心）。
  - `MAV_CMD_DO_CHANGE_SPEED` (Change Speed)：
    - Param 1 (Speed type): `1.0`（代表 Groundspeed 地速）。
    - Param 2 (Speed): `float(speed)`（m/s）。
    - Param 3 & 4: `-1.0` (不變) 與 `0.0` (絕對值)。
    - 此實作符合 MAVLink 2.0 標準，完全正確。
  - **高度變更 (Change Altitude)**：
    - 目前是透過發送帶有新高度的 `MAV_CMD_DO_REPOSITION` 實現，修正經緯度 1e7 問題後，實機高度調整功能亦將恢復正常。

#### 3. Plan 介面模式切換 (Add Waypoint / Add Survey)
* **介面設計**：
  - 在 Plan 視圖的右側 Sidebar 頂部，新增一組互斥按鈕（Radio-like Tabs）：
    - **`📍 Add Waypoint`**
    - **`🗺️ Add Survey`**
  - 使用者必須先啟用其中一個模式，雙擊地圖才會執行對應的操作。當兩者皆未啟用時，雙擊地圖不作動作，防止誤觸。

#### 4. 測繪區域邊界點劃定 (Survey Boundary Points)
* **操作邏輯**：
  - 啟用 `Add Survey` 模式後，使用者在 Leaflet 地圖上雙擊會新增一個 **Survey 邊界點**，並存入 `surveyPolygonPoints` 陣列。
  - 地圖上會實時以一條亮紫色實線將這些點依序相連。當點數大於等於 3 時，自動形成閉合的半透明多邊形區域 (`L.polygon`)。
  - 邊界標記將設為 `draggable: true`，使用者可自由拖曳調整多邊形形狀，航路將自動重算。

#### 5. 自動測繪航路生成演算法 (Lawnmower / Survey Grid Generator)
* **幾何航線生成算法**：
  - 我們將在前端實作輕量、高效的 boustrophedon (S型往返) 掃描線生成演算法。
  - **座標轉換**：將多邊形經緯度投影至平面座標系（米為單位，並以多邊形起點為投影基準點）。
  - **角度旋轉**：依使用者設定的角度 $\theta$，將所有頂點逆時針旋轉 $-\theta$，使掃描線水平。
  - **求交與配對**：在旋轉後的包圍盒 (Bounding Box) 內，以「線距」為步長由下往上劃水平線，與多邊形邊界求交點並排序配對。
  - **S型排序 (Lawnmower)**：奇偶行交替反轉點順序，形成平滑的往返折線。
  - **還原經緯度**：將所有航線點旋轉回 $\theta$ 並反投影回經緯度。
* **UI 互動與上傳**：
  - 右側 Sidebar 顯示測繪參數控制項：**線距 (Spacing, 10m~100m)**、**角度 (Angle, 0°~360°)**、**高度 (Altitude, 10m~120m)**、**起始位置反轉 (Reverse)**。
  - 地圖上以亮綠色線條預覽測繪航線。
  - 點擊 `Upload Mission` 時，自動將生成的測繪航線點（Survey Grid Points）按順序轉換為 MAVLink 任務航點，並連同一般航點一同上傳。

### Report

#### 1. Fly 視圖雙擊 Guided 觸發與防誤觸
- **實作結果**：成功將 Go-To 與 Orbit popup 觸發從單擊 (`click`) 改為地圖雙擊 (`dblclick`) 事件，並在 Fly 視圖生效。在 Fly 視圖下單擊地圖不會彈出選單，完美避免了拖曳/平移地圖時的誤觸。
- **檔案變更**：[Map.tsx](../web-ui/src/components/Map.tsx) 的雙擊事件分流與單擊 deselect 最佳化。

#### 2. MAVLink 2.0 協議參數與單位閉環 Review
- **經緯度座標修正**：修改了後端 [gateway.py](../gateway/gateway.py) 中的 `_handle_go_to` (DO_REPOSITION) 與 `_handle_orbit` (DO_ORBIT)，將原本的 `int(lat * 1e7)` 與 `int(lon * 1e7)` 徹底修正為符合 MAVLink 2.0 COMMAND_LONG 規範的單精度浮點數 **`float(lat)`** 與 **`float(lon)`**（單位為度 degree）。
- **指令格式優化**：
  - `DO_REPOSITION` 中的 `param4` (Yaw) 設為 `float('nan')` (表示不變更 Yaw 航向)，解決實機飛行做動不正常的現象。
  - `DO_ORBIT` 中的 `param2` (Speed) 設為 `float('nan')`，讓飛控自動決定合適的速度.
  - `DO_CHANGE_SPEED` 中 Param 1 為 `1.0` (地速)，Param 2 為實時 `speed` 值，經 Review 完全正確。

#### 3. Plan 視圖模式切換（Add Waypoint / Add Survey）與雙擊鎖定
- **實作結果**：在右側 Sidebar 頂端新增了 **Editor Plan Mode Selector**，提供 `📍 Waypoint` (航點編輯模式)、`🗺️ Survey` (測繪模式)、`🔒 Lock` (鎖定地圖點擊) 的互斥切換按鈕。
- **安全防護**：只有切換到對應模式雙擊地圖才會執行操作；處於 Lock 模式時雙擊不作反應，為航線規劃提供防誤觸安全機制。

#### 4. Draggable 測繪多邊形與頂點刪除
- **實作結果**：當處於 `Survey` 模式時，雙擊會在地圖加入 Survey Area 的邊界點，以半透明紫色渲染 `L.polygon` 連線面積。
- **邊界點 Handle 微調**：邊界點渲染為藍色小圓 Handles，支援 `draggable: true` 拖曳微調，拖曳時產生的航線會 60fps 實時重算並更新地圖。
- **刪除頂點**：在 Plan 視圖下直接點擊藍色頂點 Handle，即可將其移除，方便彈性修剪測繪區域。

#### 5. 自動 Lawnmower S型掃描測繪航線生成演算法
- **實作結果**：於 [surveyGenerator.ts](../web-ui/src/utils/surveyGenerator.ts) 中，獨立實作了 boustrophedon 掃描線求交幾何演算法（轉換至平面投影 $\rightarrow$ 逆旋轉 $-\theta$ $\rightarrow$ 劃平行線求交點並排序配對 $\rightarrow$ S-curve 連接 $\rightarrow$ 順旋轉 $\theta$ $\rightarrow$ 反投影回經緯度 `toFixed(6)` 輸出）。
- **實時設定與預覽**：在 Sidebar 中提供了 **Spacing (線距 10m~100m)**、**Survey Angle (角度 0°~355°)**、**Survey Altitude (高度 10m~120m)**、**Reverse Direction (反向)** 等滑動控制項，拖曳時地圖上即時以亮綠色線條實時預覽產生的航線。

#### 6. 合併任務上傳與模擬飛行驗證
- **實作結果**：在 `sendMissionUpload` 點擊上傳時，會自動將產生的測繪點（高度設為 surveyAltitude，Hold time 0 秒）拼接：
  - 若普通航點為空，自動以第一個 survey 點作為 `TAKEOFF`，隨後是測繪點，最後為 `RTL`。
  - 若已規劃普通航點，會自動將測繪航路插在最後一個 RTL/LAND 之前。
  - **模擬器支援**：上傳時也會同步寫入模擬器的 `wpsRef`，使模擬無人機在切換至 MISSION 模式後能照著產生的 S 型測繪航路模擬飛行。


## Mission 4
Fix issue:
- survey mission error
  - Status: ERROR Mission upload rejected by flight controller. Code: 3
  - px4: WARN  [mavlink] IGN MISSION_ITEM: Invalid item
- Basic Missiion error: px4
  ```
  pxh> WARN  [mission_feasibility_checker] First waypoint too far away: 12062429m, 900 max
  WARN  [mission_feasibility_checker] Mission rejected: takeoff not first waypoint item
  WARN  [navigator] mission check failed
  INFO  [tone_alarm] notify negative
  INFO  [commander] Ready for takeoff!
  INFO  [mavlink] partner IP: 127.0.0.1
  WARN  [mavlink] IGN MISSION_ITEM: Invalid item
  WARN  [mavlink] IGN MISSION_ITEM: Invalid item
  INFO  [commander] Armed by external command
  INFO  [tone_alarm] arming warning
  WARN  [navigator] Already higher than takeoff altitude
  INFO  [tone_alarm] notify negative
  INFO  [commander] Disarmed by auto preflight disarming
  INFO  [tone_alarm] notify neutral
  INFO  [logger] closed logfile, bytes written: 25947456
  INFO  [tone_alarm] notify negative
  INFO  [commander] Armed by external command
  INFO  [tone_alarm] arming warning
  INFO  [logger] Start file log (type: full)
  INFO  [logger] [logger] ./log/2026-06-20/23_47_12.ulg
  INFO  [logger] Opened full log file: ./log/2026-06-20/23_47_12.ulg
  INFO  [navigator] Using default takeoff altitude: 2.5 m
  INFO  [commander] Takeoff detected
  INFO  [tone_alarm] notify negative
  INFO  [tone_alarm] notify negative
  INFO  [tone_alarm] notify negative
  INFO  [tone_alarm] notify negative
  INFO  [tone_alarm] notify negative
  INFO  [tone_alarm] notify negative
  ```
- Mission plan mode 的 waypoint mode 沒辦法 dclick 增加 WP.
 
 請先閱讀主要描述問題後, 將TODO展開, 並將 feedback 放在 Feedback區域, 如需要回應使用者會在裡面加上 **USER:** 開頭進行回應。本次開始新增 Report功能, 實做結束之後請將 report 放在那邊。

### TODO
- [x] 修復 Plan 視圖 Waypoint 模式雙擊無法新增航點 the Bug (解決 Map.tsx 中的 React 閉包過期問題)
- [x] 修復 MAVLink 任務上傳被飛控拒絕的 Bug (兼容處理 MISSION_REQUEST 與 MISSION_REQUEST_INT，避免經緯度解碼出 1200 萬米外無效座標)
- [x] 優化任務首點 TAKEOFF 拼接邏輯，確保上傳至 PX4 的第一個任務項目必定為 TAKEOFF 指令，順利通過 navigator 檢查
- [x] 修正 Guided Takeoff 高度坐標系與經緯度參數錯誤 (讀取當前遙測 MSL 高度並加上相對高度，經緯度參數帶入當前坐標，避免 PX4 報出 Already higher than takeoff altitude 錯誤)
- [x] 修正 Guided Reposition (Go To) 與 Orbit 遙控命令，全面改用 COMMAND_INT 與 MAV_FRAME_GLOBAL_RELATIVE_ALT 解決浮點精度不足導致拒絕執行 (notify negative) 及高度解讀錯誤的問題


### Feedback

針對 **Mission 4** 的兩個核心阻礙問題，以下為您提供詳細的排查分析與修復計畫：

#### 1. 關於「Mission plan mode 的 waypoint mode 沒辦法 dclick 增加 WP」
* **問題根源**：
  - 在 [Map.tsx](../web-ui/src/components/Map.tsx) 中，地圖的雙擊 `dblclick` 事件監聽器是在 `useEffect` (地圖初始化時，dependency array 為 `[]`) 註冊的。
  - 這導致雙擊事件的回呼函數捕獲了過期 (Stale) 的 `onWaypointsChange` 與 `onSelectWp` 閉包。而在地圖剛初始化時，無人機選擇 `activeVehicleId` 還是 `null`。
  - 當使用者之後選擇了無人機並雙擊新增航點時，呼叫了過期的 `onWaypointsChange` 閉包，其內部讀取的 `activeVehicleId` 依然是 `null`，進而被 `App.tsx` 裡的 `handleWaypointsChange` 直接 return，造成新增失敗。
  - *註：Survey 模式能成功是因為我們在 previous commit 中對其 callback 採用了 `Ref` 包裝，得以獲取最新閉包。*
* **修復方案**：
  - 在 [Map.tsx](../web-ui/src/components/Map.tsx) 中，同樣使用 `useRef` 將 `onWaypointsChange` 與 `onSelectWp` 包裝起來（例如 `onWaypointsChangeRef`），並在雙擊閉包內部呼叫 `onWaypointsChangeRef.current(...)`。這能徹底解決 React 閉包滯後導致無法新增航點的問題。

#### 2. 關於「MAVLink 任務上傳失敗 (Invalid item / First waypoint too far away / takeoff not first item)」
* **問題根源 A：經緯度座標大數值錯誤 (12062429m)**
  - 在 [gateway.py](../gateway/gateway.py) 的 `_handle_upload_mission` 中，我們對於所有的任務請求，均回覆了 `mission_item_int_send`。
  - 依據 MAVLink 協議，若飛控（例如特定舊版 PX4 配置）採用 `MISSION_REQUEST` (ID 40) 來請求航點，它期待收到的回覆是舊格式的 `MISSION_ITEM` (其 lat 和 lon 是 float 度數)。而我們強行回覆了 `MISSION_ITEM_INT` (ID 73)，導致飛控將 4 字節整數解譯為單精度浮點數，使得經緯度度數放大了千萬倍，進而報出 Home 點與首點在 1200 萬米外的嚴重錯誤。
  - **修復方案**：我們在接收飛控請求時進行動態類型分支判斷。若飛控使用 `MISSION_REQUEST` 請求，我們使用 `master.mav.mission_item_send(...)` 回覆，並傳入正常的 `float(lat)`、`float(lon)`；若使用 `MISSION_REQUEST_INT` 請求，則使用 `master.mav.mission_item_int_send(...)` 回覆，傳入 `int(lat * 1e7)`、`int(lon * 1e7)`。這能保證完美的向下相容性與精準解析。
* **問題根源 B：第一個任務項目不是 TAKEOFF**
  - PX4 的可行性檢查器（feasibility checker）強制要求無人機在地面時的第一個任務項目 (seq 1, 因為 seq 0 是 Home 點) 必須是 `TAKEOFF`。如果首點是普通 `WAYPOINT` 或其他命令，任務將會被直接拒絕。
  - **修復方案**：在 [App.tsx](../web-ui/src/App.tsx) 的 `sendMissionUpload` 合併任務時，我們會執行一次安全檢驗：若上傳的任務列表首個航點不是 `TAKEOFF`，前端將會自動在最前面插入一個 `TAKEOFF` 航點（座標為首個任務點的位置，高度為首點設定的高度），以確保 100% 通過 PX4 navigator 的檢驗。

### Report

#### 1. 修復 Waypoint 模式雙擊新增航點 (Stale Closure)
- **實作結果**：成功在 [Map.tsx](../web-ui/src/components/Map.tsx) 中使用 `useRef` 將 `onWaypointsChange` 與 `onSelectWp` callback 包裝。在 map initialization 的事件註冊中，全面改為調用其 `.current` 物件（`onWaypointsChangeRef.current` / `onSelectWpRef.current`）。這完全解決了 React Stale Closure 引起的閉包滯後問題，不論在任何時候切換 Waypoint 模式，均可順利在地圖上雙擊新增航點。

#### 2. 修復 MAVLink 任務上傳相容性 (MISSION_REQUEST vs MISSION_REQUEST_INT)
- **實作結果**：修改了 [gateway.py](../gateway/gateway.py) 的 `_handle_upload_mission` 狀態機循環，對飛控傳來的航路項目請求（MISSION_REQUEST）進行分類處理：
  - 當收到 MAVLink 1.0 的 **`MISSION_REQUEST`** 時，系統將動態使用 `mission_item_send` 來回覆，並將座標反解成標準度數浮點數傳遞；
  - 當收到 MAVLink 2.0 的 **`MISSION_REQUEST_INT`** 時，系統則使用 `mission_item_int_send` 回覆整數度數（$deg \times 10^7$）。
- **效果**：解決了因 Message ID 不匹配導致 PX4 報出 `Invalid item` 以及經緯度解析度數溢出造成 `First waypoint too far away: 12062429m` 的 Bug。

#### 3. 確保任務首點必定為 TAKEOFF (PX4 Feasibility Checker)
- **實作結果**：在 [App.tsx](../web-ui/src/App.tsx) 的 `sendMissionUpload` 合併與上傳航路程序中，加入強制前置 `TAKEOFF` 檢驗：
  - 系統會實時檢查即將上傳的任務列表首個航點（非 seq 0 的 Home 點）是否為 `TAKEOFF` 司令。若不是，系統會自動在最前方無縫插入一個 `TAKEOFF` 項目，座標與首個任務點對齊，高度預設為首點高度。
- **效果**：這能 100% 確保每次點擊 Upload 上傳任務時，PX4 可行性檢查器不會因 `Mission rejected: takeoff not first waypoint item` 而拒絕任務，為實機與 SITL 提供極致穩健 of 任務傳輸。

#### 4. 修正 Guided Takeoff 一鍵起飛高度與坐標設定 (Already higher than takeoff altitude)
- **實作結果**：
  - 在 [gateway.py](../gateway/gateway.py) 遙測讀取迴圈中，新增解析 `GLOBAL_POSITION_INT` 中的 MSL 絕對高度 (`msg.alt / 1000.0`)，並將其保存於 `msl_altitude` 快取中。
  - 修改 `_handle_takeoff` 指令發送函式，發送 `MAV_CMD_NAV_TAKEOFF` 時，從快取中讀取無人機當前經緯度與 MSL 絕對高度。
  - 將 `param5` (Latitude) 與 `param6` (Longitude) 改為無人機的當前經緯度（無遙測時改用 `float('nan')`），避免 PX4 因為收到 0.0 而誤用 WGS84 原點 (0,0) 的高程 (0m) 誤判當前高度大於目標起飛高度。
  - 將 `param7` (Altitude) 的起飛高度改為當前遙測 `msl_alt + altitude`，使飛控以絕對海平面高度進行對齊升空。
- **效果**：完美解決了 Guided Takeoff 起飛失敗並提示 `Already higher than takeoff altitude` 造成 preflight 自動 disarm 的嚴重問題。

#### 5. 修正 Guided Reposition (Go To) 與 Orbit 遙控指令與高度基準 (Already higher / negative notify)
- **實作結果**：
  - 將 [gateway.py](../gateway/gateway.py) 中的 `_handle_go_to` 與 `_handle_orbit` 全面從舊式的 `command_long_send` 改為高精度的 **`command_int_send`**。
  - 在發送時，明確將座標框架 `frame` 設為 `mavutil.mavlink.MAV_FRAME_GLOBAL_RELATIVE_ALT`。
  - 將經緯度以整數型態（經緯度 $\times 10^7$）傳遞，確保符合 PX4 所要求的高精度厘米級解析標準。
  - 高度 `alt` 欄位維持相對高度直接傳入，因為已透過 `frame` 指定為 `MAV_FRAME_GLOBAL_RELATIVE_ALT`，PX4 能 100% 正確處理為相對於起飛點的相對高度，不再需要手動累加 MSL 海拔高程。
- **效果**：徹底解決了發送 Go To / Reposition 指令時，飛控因浮點精度不足而回報 `MAV_RESULT_DENIED` 並發出 `INFO [tone_alarm] notify negative` 拒絕執行的問題，同時保證了引導飛行時高度解析與設定的百分之百準確性。